/** @format */

import { addUser, deleteUser } from "./userSlice";
import { addMenuForUser, addProductInMenu } from "./menuSlice";

export const reducers = {
  addUser,
  deleteUser,
  addMenuForUser,
  addProductInMenu,
};
